<launch>
  <arg name="launch_driver" default="true" description="do launch driver"/>
  <arg name="vehicle_mirror_param_file" default="$(find-pkg-share autoware_vehicle_info_utils)/config/vehicle_mirror.param.yaml" description="path to the file of vehicle mirror position yaml"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>
  <arg name="vehicle_id" default="$(env VEHICLE_ID default)"/>

  <group>
    <!-- LiDAR Driver -->
    <include file="$(find-pkg-share eng_loading_dock_sensor_kit_launch)/launch/lidar.launch.xml">
      <arg name="launch_driver" value="$(var launch_driver)"/>
      <arg name="vehicle_mirror_param_file" value="$(var vehicle_mirror_param_file)"/>
      <arg name="pointcloud_container_name" value="$(var pointcloud_container_name)"/>
    </include>

    <!-- Camera Driver -->
    <include file="$(find-pkg-share eng_loading_dock_sensor_kit_launch)/launch/camera.launch.xml">
      <arg name="launch_driver" value="$(var launch_driver)"/>
    </include>

    <!-- IMU Driver -->
    <include file="$(find-pkg-share eng_loading_dock_sensor_kit_launch)/launch/imu.launch.xml">
      <arg name="launch_driver" value="$(var launch_driver)"/>
    </include>

    <!-- GNSS Driver -->
    <include file="$(find-pkg-share eng_loading_dock_sensor_kit_launch)/launch/gnss.launch.xml">
      <arg name="launch_driver" value="$(var launch_driver)"/>
    </include>

    <!-- Bag has no /vehicle/status/velocity_status. Derive it from NovAtel GNSS
         so gyro_odometer / pose_initializer stop-check receive twist. -->
    <node
      pkg="eng_loading_dock_sensor_kit_launch"
      exec="gnss_to_velocity_report.py"
      name="eng_dock_gnss_to_velocity_report"
      output="both"
    >
      <param name="input_gnss_topic" value="/novatel/oem7/fix"/>
      <param name="output_velocity_topic" value="/vehicle/status/velocity_status"/>
      <param name="frame_id" value="base_link"/>
      <param name="min_publish_dt_sec" value="0.04"/>
    </node>

    <!-- Vehicle Velocity Converter -->
    <include file="$(find-pkg-share autoware_vehicle_velocity_converter)/launch/vehicle_velocity_converter.launch.xml">
      <arg name="input_vehicle_velocity_topic" value="/vehicle/status/velocity_status"/>
      <arg name="output_twist_with_covariance" value="/sensing/vehicle_velocity_converter/twist_with_covariance"/>
    </include>
  </group>
</launch>
